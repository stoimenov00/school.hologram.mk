"use client";import{Delete,X}from"lucide-react";const rows=["љ њ е р т ѕ у и о п ш ѓ ж".split(" "),"а с д ф г х ј к л ч ќ".split(" "),"з џ ц в б н м".split(" ")];export function Keyboard({value,onChange,onClose}:{value:string;onChange:(v:string)=>void;onClose:()=>void}){return <div className="keyboard"><div className="keyboard-top"><b>Македонска тастатура</b><button onClick={onClose}><X/> Затвори</button></div>{rows.map((r,i)=><div className="key-row" key={i}>{r.map(k=><button key={k} onClick={()=>onChange(value+k)}>{k.toUpperCase()}</button>)}</div>)}<div className="key-row actions"><button onClick={()=>onChange(value.slice(0,-1))}><Delete/></button><button className="space" onClick={()=>onChange(value+" ")}>празно место</button><button onClick={()=>onChange("")}>Избриши</button></div></div>}
