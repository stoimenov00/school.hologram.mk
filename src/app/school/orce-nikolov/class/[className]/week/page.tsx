"use client";import{useParams}from"next/navigation";import{KioskShell}from"@/components/KioskShell";import{timetable}from"@/data/schools/orce-nikolov";const days=[{k:"monday",l:"Понеделник"},{k:"tuesday",l:"Вторник"},{k:"wednesday",l:"Среда"},{k:"thursday",l:"Четврток"},{k:"friday",l:"Петок"}]as const;export default function Week(){const{className}=useParams<{className:string}>();const c=decodeURIComponent(className);return <KioskShell title={`Неделен распоред · ${c}`} subtitle="Мој распоред"><div className="week-grid">{days.map(d=><section key={d.k}><h2>{d.l}</h2>{timetable.filter(l=>l.className===c&&l.day===d.k).map(l=><div key={l.id}><b>{l.period}</b><span><strong>{l.subject}</strong><small>{l.startTime} · Кабинет {l.roomId}</small></span></div>)}</section>)}</div></KioskShell>}
